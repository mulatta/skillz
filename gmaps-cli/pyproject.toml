[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gmaps-cli"
version = "0.1.0"
description = "CLI tool to manage Google Maps lists and places"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
gmaps-cli = "gmaps_cli:main"

[tool.hatch.build.targets.wheel]
only-include = ["gmaps_cli.py", "py.typed"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
  "ARG001",
  "COM812",
  "D",
  "E501",
  "ERA001",
  "FBT001",
  "FBT002",
  "FIX",
  "G004",
  "ISC001",
  "PLR2004",
  "S101",
  "S603",
  "S607",
  "T201",
  "TD",
]

[tool.mypy]
python_version = "3.12"
strict = true
